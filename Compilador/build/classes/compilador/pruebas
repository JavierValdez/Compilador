
set b 2
set c [expr $a + $b]
#Javier

if {$a > 0} {
	,-.
	
	for {set x 0} {$x < $c} {incr i} {
		puts "El valor de x es $x"
	}


	switch x {
		"1" {
			puts "Uno"
		}
		"2" {
			puts "Dos"
		}
		"3" {
			puts "Tres"
		}
		default {
			puts "El numero, es mayor que tres"
		}

	}
}
     {
expr
puts
set a 1
set b 2
abc
}